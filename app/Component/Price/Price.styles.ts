import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    gap: 0,
  },
  disabledPrice: {
    textDecorationLine: 'line-through',
  },
  discountPercentage: {
    fontWeight: 'bold',
  },
});
